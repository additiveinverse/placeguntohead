extends layout

block content
	include author
	each article in articles
		article
			header
				h2
					a(href=article.url)= article.title

				p.date
					span= moment.utc(article.date).format('MM-DD')

			| !{ typogr(article.intro).typogrify() }
